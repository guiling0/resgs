# AGENTS.md

本文件是项目的智能体规则，为在本项目内工作的 AI 提供行为指引与约束。所有支持 AGENTS.md 的 AI 智能体都应遵循本文件，规则随项目发展持续演进。

## 沟通约定

- 永远使用简体中文进行思考和对话
- 编写 .md 文档时也要用中文

## 文档体系

项目文档分两部分：`knowledge/`（知识库，唯一知识权威源）与 `docs/`（项目管理），遵循「**按需写作、索引防膨胀、单一权威**」三项原则。

### 知识库（knowledge/）

- 游戏规则（`rules/`：definitions / events / terms / topics，词条带 `id`）、项目 API（`project-api/`，`kb:api` 自动生成）、扩展文档（`extensions/`：modes / cards / generals / projects）、编写指南（`guide/`）
- 入口见 `knowledge/README.md`；`generated/` 为自动生成区（`kb:build` 重建，勿手改）
- 规则词条/事件时机是唯一权威：技能归档、时机穷举均写入 `knowledge/rules/`

### 项目管理（docs/）

| 内容性质 | 存放位置 | 说明 |
|---|---|---|
| 决策 | `docs/decisions/` | 技术选型、架构决策、实现方案等有真实权衡的决策，记录理由与结论 |
| 计划与执行 | `docs/planning/` | 三级递进：`plans/`（需求计划）→ `specs/`（计划的完整细化）→ `issues/`（单 spec 的步骤拆分与验收） |
| 会话交接 | `docs/sessions.md` | 重要会话的交接索引（见下） |

> 项目进度、裁定等内容不写入知识库，保持 `docs/` 现有规则。

### 写作规则

1. **先索引后内容**：新写的规则/扩展文档先入知识库（`kb:index` 自动聚合索引），读者按索引按需读取，不要求通读全部文档
2. **按需阅读**：开始工作前只读与当前任务相关的文档，不加载无关内容；规则术语从 `knowledge/rules/` 索引进入
3. **单一权威**：同一概念的定义只保留在一处权威文档，其余位置用链接引用，不复制内容
4. **会话交接**：重要会话结束时，在 `docs/sessions.md` 登记一条记录（会话标识、日期、工具、主题、关键结论、关联文件）——只记指针，不复制纪要全文（完整纪要见 `~/.trae-cn/memory/` 下 `session_memory_*.jsonl`，跨端可读）
5. **不记过程**：不写讨论过程类文档，不保留会话日志
6. **规范归本文件**：工程规范、操作流程、代码约定等约束写入 AGENTS.md，不另设文档

## 技术栈

| 层 | 技术 |
|---|---|
| 共享逻辑 | TypeScript（`shared/`，服务端/客户端共用，纯 TypeScript 无网络依赖） |
| 服务端 | Node.js + Colyseus 0.17 |
| 客户端 | LayaAir 3.4（`client/`，UI2 系统 + 引擎内置渲染/动效/音效）+ Colyseus SDK |
| 数据库 | MongoDB |
| 资料站 | 纯 HTML/CSS/JS（`wiki/`） |

## 资源与素材

- `.tmp/resgs-assets/` 存放当前 CDN 上的全部素材，仅作素材源（参考/迁移用），不参与构建
- 动态资源按名称注册（牌名/武将真名/技能真名），未配置字段走默认路径模板（见 `shared/core/utils/AssetsUtils.ts`）；注册入口见 `sgs.registerCardAssets` / `sgs.registerGeneralAssets`
- 写卡牌/武将扩展时，需同步配置资源信息（CardAssets/GeneralAssets），并将 `.tmp` 中对应素材移动到资源目录，命名与路径符合默认值约定：

| 资源 | 默认路径 |
|---|---|
| 牌图 | `image/cards/{牌名}.png` |
| 卡牌配音 | `audio/card/{male\|female}/{牌名}.mp3` |
| 武将插画 | `generals/{武将真名}/{皮肤名}/image.png` |
| 他人视角插画 | `generals/{武将真名}/{皮肤名}/image_dual.png` |
| 自己视角插画 | `generals/{武将真名}/{皮肤名}/image_dual_self.png` |
| 阵亡语音 | `generals/{武将真名}/{皮肤名}/death.mp3` |
| 技能语音 | `generals/{武将真名}/{皮肤名}/{技能真名}{序号}.mp3` |

- 旧 CDN 素材为平铺结构（如 `generals/{name}/image.png`、技能语音 `pozhu1.mp3` 直接平铺），迁移到新默认值需补皮肤层级（`{name}/{skin}/...`）并按上述约定归位
- 皮肤配置基于武将真名共享；同名武将不同版本仅默认皮肤不同（`GeneralData.defaultSkin`）

## 共享代码约定

- `shared/` 内模块引用使用别名 `@shared/*`（如 `import { GameCard } from '@shared/core/card/GameCard'`）
- 核心游戏文件作为纯 TypeScript 实现，不依赖任何外部模块
- 根目录 `tsconfig.json` 仅用于类型检查，不参与实际构建
- `sgs` 全局：`globalThis.sgs` 持有全部静态数据，除初始化时必要的 import 外，其余位置直接 `sgs.xxx` 访问
- `sync` 参数：暂未实现

## 当前工作焦点

（待聚集）

## 技能学习义务

### 目的

本项目的扩展编写技能服务无编程基础的开发者：通过自然语言让 AI 编写自定义游戏内容（每个合集称为"扩展"）。技能需要持续学习、沉淀，才能越用越懂。

### 触发时机

用户向 `extension/` 写入新的扩展内容（牌使用数据、规则技能、武将技能）时。

### 学习流程

1. **学习总结**：分析新扩展的结构与编写模式，提炼可复用的写法与惯例
2. **归档**（遵循 `knowledge/extensions/index.md` 的「扩展完成后归档流程」）：
   - 更新扩展文档（`knowledge/extensions/`：projects 扩展说明、generals / cards 归档，内容以代码 `GeneralData` 属性为准）
   - 学习沉淀写入 `knowledge/guide/`（不重复学习：已归档过的描述、代码结构不重复总结）
   - 出现新的游戏规则概念时，同步更新 `knowledge/rules/`（遵循「单一权威」原则）
   - 针对每一个技能，放入规则文档的时机穷举或词条穷举中
3. **双向转换**：维护「标注描述」（人类可读）↔「详细描述」（代码可执行）的对应关系
4. **自测校验**：基于更新后的定义编写/验证后续扩展，确保定义可用

### 约束

- 学习动作遵循「改动确认制」：先说明将更新哪些文件，确认后执行
- 不写入旧项目对比、方案讨论类内容
- 项目进度、裁定等内容保持 `docs/` 现有规则，不写入知识库

## 编码规则

### 改动确认制

- **先方案后动手**：任何文件修改前，先列出改动方案（涉及文件、改动内容、原因），经确认后才执行
- **分批确认**：每完成一批改动，展示改动摘要供审查，确认后再进入下一步
- 未获确认前不修改文件

### 代码审查

- 仅在用户主动提出时执行
- **先审查不修改**：列出全部问题，按严重度分级（阻塞 / 建议 / 风格）
- 由用户逐条决定是否修改

### 代码优化

- 仅在用户主动提出时执行
- **先方案后修改**：给出优化方案（改动内容、理由、影响面），确认后再改

### Git 提交

- **提交文案由 AI 生成**：任何提交（含用户要求提交时）都先由 AI 生成提交文案
- 文案遵循 Conventional Commits 风格：`feat:` `fix:` `docs:` `refactor:` `chore:` `test:` 等前缀 + 中文描述
- 提交前展示文案供用户确认

### 注释原则

- 使用 JSDoc 注释（`/** ... */`），格式固定：

**类**

```ts
/**
 * 简要说明
 * @rules [链接的规则事件]
 * @description 具体说明
 */
```

**方法**

```ts
/**
 * 简要说明
 * @rules [链接的规则词条]
 * @description 具体说明
 * @param [依次列出所有参数]
 * @returns [返回值]
 */
```

**属性**

```ts
/**
 * 简要说明
 * @rules [链接的规则词条]
 * @description 具体说明
 */
```

- `@rules` 值为知识库规则词条/事件时机 id（如 `@rules events/pindian`、`@rules terms/card-op-terms/qizhi`）
- 函数体内部的关键步骤使用单行流程注释（`// ...`）
- **禁止**写任何非实现级说明：旧项目对比、实现方案讨论、设计决策辩护、修改前后差异，以及「为什么」——只写最简单直接的规则性说明

### 日志与调试

- shared 内调试输出统一经房间级 logger（`room.logger`）发出，不直接使用 console
- server/client 层可能不存在 Room 上下文，使用各自日志设施（服务端 Winston / 客户端 console），不受房间级 logger 限制
- 关键节点补充调试输出：函数调用入口、复杂逻辑分支、关键状态变化
- 按级别分层：`debug`（细节过程）、`info`（关键流程）、`warn`（非致命异常）、`error`（错误）
- 发布环境过滤低级别（debug/info）输出，避免噪音；线上服务端按对局单文件记录（Winston），便于回溯 bug
- 调试输出服务于测试脚本运行观察与线上问题定位

### 日志输出格式

- 固定字段（`roomId`/`playerId`/`event`）前置 `[]` 标签：`[Room:r1][Player:p2][draw] 摸牌 {"count":2}`
- 固定字段之外的可选字段以 JSON 追加在消息之后
- 两端控制台均着色：服务端 ANSI（Room 青 / Player 黄 / event 紫），客户端浏览器 console 用 `%c` + CSS（同色）；服务端文件输出为纯文本
- 两端实现（服务端 Winston / 客户端 ConsoleLogger）格式一致

### 代码规范

编写和审查代码时，关注以下可能侵蚀代码质量的「坏味道」，识别后提出优化建议并询问是否处理：

1. **僵化**：微小改动引发连锁修改
2. **冗余**：同样逻辑在多处重复
3. **循环依赖**：模块互相纠缠无法解耦
4. **脆弱性**：修改一处导致看似无关部分损坏
5. **晦涩性**：代码意图不明，结构混乱
6. **数据泥团**：多个数据项总是一起出现，应组合成对象
7. **不必要的复杂性**：过度设计

### TypeScript 约定

- 尽可能使用 TypeScript
- 数据结构尽可能定义为强类型
- 个别场景必须使用 `any` 或非结构化 JSON 时，先征求用户同意

### 测试

- 仅在用户要求时编写测试并运行
- 只运行相关套件，不跑全量测试