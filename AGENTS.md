# AGENTS.md

本文件是项目的智能体规则，为在本项目内工作的 AI 提供行为指引与约束。所有支持 AGENTS.md 的 AI 智能体都应遵循本文件，规则随项目发展持续演进。

## 沟通约定

- 永远使用简体中文进行思考和对话
- 编写 .md 文档时也要用中文

## 文档体系

所有项目文档位于 `docs/`，按内容性质分类存放，遵循「**按需写作、索引防膨胀、单一权威**」三项原则。

### 分类与存放

| 内容性质 | 存放位置 | 说明 |
|---|---|---|
| 领域知识 | `docs/domain/` | 游戏规则权威定义：`definitions/`（基础定义）、`events/`（事件定义）、`terms/`（用语定义） |
| 决策 | `docs/decisions/` | 技术选型、架构决策、实现方案等有真实权衡的决策，记录理由与结论 |
| 计划与执行 | `docs/planning/` | 三级递进：`plans/`（需求计划）→ `specs/`（计划的完整细化）→ `issues/`（单 spec 的步骤拆分与验收） |
| 会话交接 | `docs/sessions.md` | 重要会话的交接索引（见下） |

### 写作规则

1. **先索引后内容**：新写的领域文档先在 `docs/domain/index.md` 登记索引，读者按索引按需读取，不要求通读全部文档
2. **按需阅读**：开始工作前只读与当前任务相关的文档，不加载无关内容；领域术语从 `docs/domain/index.md` 索引进入
3. **单一权威**：同一概念的定义只保留在一处权威文档，其余位置用链接引用，不复制内容
4. **会话交接**：重要会话结束时，在 `docs/sessions.md` 登记一条记录（会话标识、日期、工具、主题、关键结论、关联文件）——只记指针，不复制纪要全文（完整纪要见 `~/.trae-cn/memory/` 下 `session_memory_*.jsonl`，跨端可读）
5. **不记过程**：不写讨论过程类文档，不保留会话日志
6. **规范归本文件**：工程规范、操作流程、代码约定等约束写入 AGENTS.md，不另设文档

## 技术栈

| 层 | 技术 |
|---|---|
| 共享逻辑 | TypeScript（`shared/`，服务端/客户端共用，纯 TypeScript 无网络依赖） |
| 服务端 | Node.js + Colyseus 0.17 |
| 客户端 | LayaAir 3.4（`client/`，UI2 系统 + 引擎内置渲染/动效/音效）+ Colyseus SDK |
| 数据库 | MongoDB |
| 资料站 | 纯 HTML/CSS/JS（`wiki/`） |

## 共享代码约定

- `shared/` 内模块引用使用别名 `@shared/*`（如 `import { GameCard } from '@shared/core/card/GameCard'`）
- 核心游戏文件作为纯 TypeScript 实现，不依赖任何外部模块
- 根目录 `tsconfig.json` 仅用于类型检查，不参与实际构建
- `sgs` 全局：`globalThis.sgs` 持有全部静态数据，除初始化时必要的 import 外，其余位置直接 `sgs.xxx` 访问
- `sync` 参数：暂未实现

## 当前工作焦点

（待聚集）

## 技能学习义务

### 目的

本项目的扩展编写技能服务无编程基础的开发者：通过自然语言让 AI 编写自定义游戏内容（每个合集称为"扩展"）。技能需要持续学习、沉淀，才能越用越懂。

### 触发时机

用户向 `extension/` 写入新的扩展内容（牌使用数据、规则技能、武将技能）时。

### 学习流程

1. **学习总结**：分析新扩展的结构与编写模式，提炼可复用的写法与惯例
2. **更新权威定义**：
   - 更新技能知识库（`.trae/skills/sgs-extension/references/` 下对应文件）
   - 出现新的游戏规则概念时，同步更新领域文档（`docs/domain/`，遵循「单一权威」原则）
3. **双向转换**：维护「标注描述」（人类可读）↔「详细描述」（代码可执行）的对应关系
4. **自测校验**：基于更新后的定义编写/验证后续扩展，确保定义可用

### 约束

- 学习动作遵循「改动确认制」：先说明将更新哪些文件，确认后执行
- 不写入旧项目对比、方案讨论类内容

## 编码规则

### 改动确认制

- **先方案后动手**：任何文件修改前，先列出改动方案（涉及文件、改动内容、原因），经确认后才执行
- **分批确认**：每完成一批改动，展示改动摘要供审查，确认后再进入下一步
- 未获确认前不修改文件

### 代码审查

- 仅在用户主动提出时执行
- **先审查不修改**：列出全部问题，按严重度分级（阻塞 / 建议 / 风格）
- 由用户逐条决定是否修改

### 代码优化

- 仅在用户主动提出时执行
- **先方案后修改**：给出优化方案（改动内容、理由、影响面），确认后再改

### Git 提交

- **提交文案由 AI 生成**：任何提交（含用户要求提交时）都先由 AI 生成提交文案
- 文案遵循 Conventional Commits 风格：`feat:` `fix:` `docs:` `refactor:` `chore:` `test:` 等前缀 + 中文描述
- 提交前展示文案供用户确认

### 注释原则

- 使用 JSDoc 注释（`/** ... */`）：类（作用）、方法（作用、参数、返回值）、属性（描述）
- 函数体内部的关键步骤使用单行流程注释（`// ...`）
- **禁止**写非实现级注释：旧项目对比、实现方案讨论、设计决策辩护、修改前后差异

### 代码规范

编写和审查代码时，关注以下可能侵蚀代码质量的「坏味道」，识别后提出优化建议并询问是否处理：

1. **僵化**：微小改动引发连锁修改
2. **冗余**：同样逻辑在多处重复
3. **循环依赖**：模块互相纠缠无法解耦
4. **脆弱性**：修改一处导致看似无关部分损坏
5. **晦涩性**：代码意图不明，结构混乱
6. **数据泥团**：多个数据项总是一起出现，应组合成对象
7. **不必要的复杂性**：过度设计

### TypeScript 约定

- 尽可能使用 TypeScript
- 数据结构尽可能定义为强类型
- 个别场景必须使用 `any` 或非结构化 JSON 时，先征求用户同意

### 测试

- 仅在用户要求时编写测试并运行
- 只运行相关套件，不跑全量测试