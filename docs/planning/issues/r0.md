# R0 引擎地基 — 步骤拆分

> 所属 spec：[specs/r0-engine-foundation.md](../specs/r0-engine-foundation.md) · 计划：[plans/porting-map.md](../plans/porting-map.md)

| 编号 | 步骤 | 验证 | 状态 |
|---|---|---|---|
| R0-01 | **装饰器最小原型**：`@sync` 单字段 + `@syncMap` 单容器冒烟——确认 legacy 装饰器 + `useDefineForClassFields:false` 下字段初始化顺序 | 原型冒烟跑通，patch 正确 | ✅ 已完成 |
| R0-02 | **StateStore 核心**：pending 收集 + 帧级 flush（16ms tick）+ `beginBatch/endBatch` + apply 枢纽 | 冒烟：setter→flush→apply 回放一致 | ✅ 已完成 |
| R0-03 | **StatePatch 类型 + 容器**：`set/map.add/map.remove/arr.insert/arr.remove/replace`；StateMap/StateArray/StateNode（嵌套注入 `_store`/`_path`） | 冒烟：map/arr 增删产正确 patch | ✅ 已完成 |
| R0-04 | **实体层**：GameCard/VirtualCard/General/Player/Skill/Effect + MarkHost 通用化（任意实体可存键值标记） | tsc；实体字段与旧 shared 对照 | ✅ 已完成（六实体均具备标记能力：Player/General/Skill/Effect 继承 Mark，GameCard/VirtualCard 经 ICard 继承；实体分层 entity/types/builder/utils、区域 Area/Random、动态资源系统、Skill/Effect 实体与 SkillBuilder/EffectBuilder 已实现并验证；skill/effect 行为执行留 R3；遗留：实体状态对照表（与备份 *State 逐字段核对）与 spec 描述同步（Mark 继承/id number/区域 getter）留后续增量） |
| R0-05 | **事件框架**：EventProcess（eventTriggers/endTriggers + exec）+ EventTypes（TimingName 80+ const enum）+ EventManager（trigger 调度/refreshs/效果索引/PriorityType） | 单元冒烟：最小事件触发，优先级顺序正确 | ✅ 已完成（2026-08-05：EventProcess/EventManager + 13 事件类落地 logic/event/；触发按 General→Equip→Card→Rule 优先级调度；smoke-event 19 项通过；Pindian 事件留 R5，needUseCard/多效果询问留 R2）<br>2026-08-06 审查修正：Effect.check 与 canTrigger 分层（check 仅通用检测，子类无覆盖）；触发配置执行方法归位 TriggerEffect；getMaxTimes/canAutoExecute/isViewAsOrPlayPhase 迁入 TriggerEffect；GameCard/General put 标记 @sync；event 目录注释规范修复 |
| R0-06 | **sgs 全局 + register 重写**：枚举/Builder/factories 挂载（幂等），API 面与 `global.d.ts` 契约逐项核对 | 契约核对表；register 幂等 |  |
| R0-07 | **传输层补齐**：codec（serialize/deserialize）+ Envelope/MessageType 骨架 + LocalTransport 缺口修复 | tsc；最小往返序列化一致 | ✅ 已完成 |
| R0-08 | **扩展加载验证**：resgs-ext-temp 导入后 sgs.cards/generals/skills/effects 有数据，无崩溃 | 加载冒烟通过 |  |
| R0-09 | **R0 验收**：独立 tsconfig tsc + 全部冒烟脚本 + 事务原子性 + 回放一致 + 标记系统验收（mark-system-design.md 第五章） | plans「单机核心」前置就绪，进入 R1 |  |
