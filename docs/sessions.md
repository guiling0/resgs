# 会话交接索引

跨端（TRAE IDE / TRAE Work）工作交接的会话索引。**重要会话结束时登记一条**，用于快速定位该会话的完整纪要。

完整纪要存于 `%userprofile%/.trae-cn/memory/` 下的 `session_memory_*.jsonl`（按"日期 + 会话 ID"命名，跨端可读），本文件只登记**指针 + 关键结论**，不复制纪要全文。

## 格式

| 会话 | 日期 | 工具/模式 | 主题 | 关键结论 | 关联文件 |
|---|---|---|---|---|---|
| `<会话标识>` | `YYYY-MM-DD` | `ide` / `work` | 一句话主题 | 交接要点 | 涉及文件路径 |

## 记录

| 会话 | 日期 | 工具/模式 | 主题 | 关键结论 | 关联文件 |
|---|---|---|---|---|---|
| docs-重构-20260802 | 2026-08-02 | ide | 文档体系重构 | 文档按三类重组：domain（领域知识）/decisions（决策）/planning（计划执行）；Claude 残留已清除，规则迁至 AGENTS.md，技能迁至 .trae/skills/；CLAUDE.md 暂留待阅读后删除 | AGENTS.md、docs/、.trae/skills/ |
| rules-迁移-20260802 | 2026-08-02 | ide | 规则迁移 | AGENTS.md 建立完整规则（沟通/文档体系/技术栈/共享代码/编码规则/技能学习义务）；git 提交文案规则在 .trae/rules/git-commit-message.md；CLAUDE.md 已删除；工作区已提交 | AGENTS.md、.trae/rules/ |
| 重构计划-superpowers-20260802 | 2026-08-02 | ide | 重构计划重制（superpowers 工作流） | 两轮 brainstorming 对齐：完全追齐（核心+扩展+服务端+客户端）、扩展新 API 重写+技能自主学习、代码驱动+生成器、全量 20+ 包、核心先行；增量重新划分为 R0-R10（M1 单机核心 → M2 标准内容+数据管线 → M3 扩展模式 → M4 客户端/服务端）；三级文档落盘 plans（porting-map 重写）+ 11 specs + 11 issues；shared 仍处 R0 起点（仅 9 骨架文件） | docs/planning/plans/porting-map.md、docs/planning/specs/、docs/planning/issues/ |
| R0-state-transport-mark-20260803 | 2026-08-03 | ide | R0 状态层 + 传输层 + 标记系统 | R0-01/02/03（state 层：装饰器/容器/StateStore/applyPatches）✅、R0-07（传输层：ITransport 发送控制抽象类 + LocalTransport + 消息协议 snapshot/patches/event/batch + Envelope）✅；R0-04 实体层进行中：范围已确认（r0-04-entity-layer.md：迁移 entity/、id 统一 string、Player 最小集、派生 getter 基础版、无区域引用），Mark 抽象类完成（继承链模式，key 编码协议：key@tag[:data][-when/--when]，@card/@general 值转 id、@never 豁免、生命周期清理、部分可见走权威端 _visibility）；六实体（Player/GameCard/VirtualCard/General/Skill/Effect）待实现；发送时机控制上移 ITransport；标记键协议文档 docs/develop/mark-key.md；注释/日志规范已入 AGENTS.md 与项目记忆 | shared/core/state/、shared/core/transport/、shared/core/entity/Mark.ts、shared/core/ILogger.ts、shared/core/ConsoleLogger.ts、docs/planning/specs/r0-04-entity-layer.md、docs/develop/mark-key.md、docs/planning/issues/r0.md |
| R0-entity-mark-20260803 | 2026-08-03 | ide | 实体分层迁移 + Mark 系统语义完善 | 实体迁移（ADR 0004）：Room/RoomOptions/GameState/Player 移入 entity/，纯类型定义集中至 types/；Room/Player 继承 Mark（Player 复用父类 marks 容器，marks 同步 path=marks 经冒烟验证）；Mark 语义完善：getMark 忽略 tags/lifes 按主键从 marks 读取、setMark 同主键变体覆盖（旧变体删除）、ref 由 set 时标签回溯、data 数组备份 slice、索引简化 Map<string,string>；R0-04 进行中（Mark + Room/Player 就位，其余四实体待实现） | shared/core/entity/、shared/core/types/、shared/test/smoke-mark.test.ts、docs/planning/specs/r0-04-entity-layer.md、docs/planning/issues/r0.md |
| R0-entity-assets-20260804 | 2026-08-04 | ide | 实体层补全（Card/General/区域）+ 动态资源系统 | Card 实体（CardTypes/CardBuilder/GameCard/VirtualCard，ICard 抽象基类 + sourceData 保留）、General 实体 + GeneralBuilder（defaultSkin 默认 'default'、config 携带）、区域系统（Area 不继承 StateNode + Random mulberry32 + Room.randomSeed/shuffle/randomInt）；动态资源系统：默认路径模板（AssetsUtils）、CardAssets/GeneralConfig 注册、generalInfoMap（武将全名→info 全字段注入翻译表）+ generalSkinMap（武将真名→skins push 去重）、skills 只写翻译表、皮肤 audios（death 键 + 技能真名键）；翻译键约定 general.{武将名}.{字段}/skill.{技能全名}.name|desc|desc2/skill.{真名}.{皮肤}.{技能}{序号}；GeneralData.config + registerGeneralPack 一并注册资源；ADR 0005 时光回溯落盘；注释清理（去"见 xxx"/将来式）；R0-04 状态更新（Skill/Effect 待技能框架） | shared/core/entity/（GameCard/General/ICard/VirtualCard/Area/Player/Room）、shared/core/builder/、shared/core/types/（AssetsTypes/CardTypes/GeneralTypes/AreaTypes）、shared/core/utils/（AssetsUtils/Random）、shared/core/sgs.ts、shared/test/smoke-assets.test.ts、docs/decisions/adr/0005-time-travel-replay.md、docs/planning/issues/r0.md、AGENTS.md |
| skill-类型落位-20260804 | 2026-08-04 | ide | Skill/Effect 类型落位 + 选择系统现状查询 | 类型文件落位：EventTypes（TimingName 全量 + EventType + 事件数据 + TimingData<T> 两级推断链 + Timing + DamageType）、SkillTypes（SkillData/EffectData/StateCallbackMap 全量）、SelectTypes（最小集）、PlayerTypes（Phase）、RichText（RichString/RichStringValue）；CardTypes 补 VirtualCardData；entity/Skill + entity/Effect 最小骨架（@sync 字段 + Mark + getter）；typecheck shared 全绿，遗留 build-types.ts 旧路径报错；选择系统无专项 spec——散落于 R2（choice 消息/SelectSession wire）/R3（技能 choice）/R4（选择 UI）/porting-map 风险4/ADR0001/domain-index；命名决策：**Choose 为主**（ChooseManager/ChooseSession/SelectorConfig/ChooseTypes）；SelectTypes 暂留最小集保证导入；用户在 work 端详细讨论选择系统并落盘文档（纪要见 memory 20260804） | shared/core/types/（EventTypes/SkillTypes/SelectTypes/PlayerTypes/RichText/CardTypes）、shared/core/entity/（Skill/Effect）、docs/planning/plans/porting-map.md、docs/planning/specs/r2-battle-death.md、docs/planning/specs/r3-skill-framework.md、docs/planning/specs/r4-solo-closed-loop.md、docs/decisions/adr/0001-client-pixi-vite.md、docs/domain/index.md |
| choose-spec-20260804 | 2026-08-04 | ide | 选择系统重建专项 spec + AI 架构 | 完整设计落盘：命名定案（Choose 为主）、核心对象（ChooseSession/ChooseData/ChooseResult/SelectorConfig）、超时链、序列化（toWire/fromWire + 函数压缩 hash + 服务端按玩家追踪）、ChooseManager 三种发送模式（request/race/all）、便捷 API 三种行为分支、消灭 sgs.selectors、可序列化函数约束；AI 架构吸收 FreeKill（策略类型分类+卡牌价值+优先级排序）与元数据声明式（布尔标签+决策函数），零模拟评估；SkillAI 挂技能注册（R3），AIPlayer 引擎读 SkillAI + 兜底策略（R2）；全部 R2 落地 AIPlayer+ChooseManager，R3 技能附带 SkillAI，R4 客户端 UI | docs/planning/specs/choose-system-rebuild.md、shared/core/types/ChooseTypes.ts（待建）、shared/core/logic/manager/ChooseManager.ts（待建）、shared/core/logic/ai/AIPlayer.ts（待建） |
| 实体分层修订-20260805 | 2026-08-05 | ide | 实体分层修订（query 并入 entity + 能力注入） | 分层定为 `entity/`（数据+查询+能力薄转发，两端）/ `registry/`（静态注册表）/ `logic/`（PlayerHost 能力实现 + RoomEngine + 事件栈）/ `view/`（PlayerView 镜像能力实现）；实体非查询能力经 PlayerHost/PlayerView 双接口注入，host/view 仅注入其一、`host ?? view ?? 抛错` 分发；隔离 = 运行时注入差异（shared 两端同源、单机客户端即 host，取消「客户端产物不含 logic/编译期隔离」表述）；查询（getCard/getCards）直接放实体本体；Area 两端镜像一致（移动消息 + 通用 add/remove 同步，random 索引权威端定）；继承/独立逻辑对象方案因 getPlayer 查询归属与类型擦除问题否决 | docs/decisions/adr/0004-entity-layering.md、docs/planning/plans/porting-map.md、docs/planning/specs/r0-engine-foundation.md、docs/planning/specs/r0-04-entity-layer.md、shared/core/entity/Room.ts |
| R0-实体层收尾-20260805 | 2026-08-05 | ide | R0 实体层索引/查询/构造收尾 + 分层落地 | Room 全索引与查询：实体牌（cards/cardNames/cardNamesToType/cardNamesToSubType + getCard/getCards/getCardIds/getCardNames 族）、武将（generals/generalNames + getGeneral 族/getGeneralByName）、技能/效果（skills/effects + skillsByName/effectsByName 集合 + triggerEffectsById/stateEffectsById + triggerEffectsByTiming/stateEffectsByType 结构 + get 族）、玩家（getPlayer/getPlayers/getPlayerIds/alives/filterPlayer/countPlayer/sortPlayer/sortResponse/sortClockwise 按 currentPlayerId 起点）、区域快捷 getter（drawArea 等）；Player 补 role/kingdom/gender/death/phase/inturn + 派生 getter（alive/inthp/losshp/handMax/attackRange/distanceTo）+ 区域访问（getAreaId/getHandCards/getEquipCards/getJudgeCards/getSelfCards/getAreaCards）+ @sync currentPlayerId（TODO(R1) 回合流程维护）；Skill/Effect：abstract Effect + TriggerEffect/StateEffect 桩类（TODO(R3) 行为）、自增 id number（同技能多实例不覆盖，扩展包契约 SkillId/EffectId=number）、skillsByName/effectsByName 集合索引、引用改对象（player/skill/sourceGeneral/sourceEquip/sourceEffect）、同步字段裁剪（Skill 仅 preshow/showui/invalids，固定数据经创建消息）、构造传 SkillData/EffectData + options（source 推断来源、data 注入 options.data、sourceData 保留注册定义）、TODO(事件系统) autoRemove/refreshs；虚拟牌：不建全局列表/无 id/card.vcard 删除/仅权威端创建（toData 供镜像端消费 VirtualCardData）/logic/room/VirtualCardHost + RoomHost（implements VirtualCardAbility、createVirtualCard 重载、mixin 组合注入）；注释规则修复（去将来式、TODO 标注）；**register.ts 澄清：extension/registry.ts 为扩展包入口，明确不在现阶段范围** | shared/core/entity/（Room/Player/Skill/Effect/TriggerEffect/StateEffect/GameCard/General/Area/VirtualCard）、shared/core/logic/room/（RoomHost/VirtualCardHost）、shared/core/types/SkillTypes.ts、shared/core/entity/Room.ts、docs/planning/logs/README.md |
