# 扩展编写 skill

触发条件：用户需要编写三国杀游戏扩展——创建武将、技能、卡牌、模式。
此 skill 提供 Builder API 指引、文件结构约定、注册流程和发布步骤。

## 核心原则

1. **隔离环境**——扩展通过 `sgs.xxx` 访问核心 API，不 import 核心模块
2. **导入即注册**——Builder `.register()` 自动写入 `sgs.*` 注册表
3. **幂等**——重复加载同一扩展不重复注册
4. **代码/数据分离**——官方扩展可拆分为代码扩展（开发者）和数据扩展（协作者）
