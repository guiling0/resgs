---
title: 判定事件
type: event
id: events/judge
tags: [判定, 事件]
---

# 判定事件

角色判定的流程：

判定结算中有：判定时、成为判定牌后、判定结果确定前1、判定结果确定前2、判定结果确定后1、判定结果确定后2六个时机。

### 判定时

确定判定牌：进行判定的角色亮出牌堆顶的一张牌，称为“其判定牌”。

### 成为判定牌后

### 判定结果确定前1

### 判定结果确定前2

然后确定此次判定的结果。生效后的判定牌的花色、点数、名称等信息称为此次判定的“结果”。

### 判定结果确定后1

### 判定结果确定后2

至此判定结算结束。

判定结算后有：判定结算结束后一个时机。

### 判定结算结束后


至此判定流程结束。

<!-- kb:refs:start -->

## 引用区

① API 实现：
- [JudgeEvent](../../project-api/event/JudgeEvent.md)
- [EventTypes](../../project-api/types/EventTypes.md)

② 扩展信息：
  （暂无）

③ 编写指南：
  （暂无）


<!-- kb:refs:end -->
