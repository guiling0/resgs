---
title: 拼点事件
type: event
id: events/pindian
tags: [拼点, 事件]
---

# 拼点事件

角色拼点的流程：

拼点结算中有：进行拼点时、拼点牌被亮出时、（连续若干个）拼点结果确定后这些时机。

### 进行拼点时

拼点的发起者选择拼点的合法目标（从当前回合角色开始按逆时针方向对这些目标进行从小到大的编号：T1、T2……Tn）。

◆一名角色发起拼点的合法目标是有手牌的其他角色。若未说明能选择的目标的数量，即默认目标数上限为1。

◆没有手牌的角色不能发起拼点。

然后此次拼点的发起者和所有目标同时各从各自选择此次拼点的牌的范围中（角色选择拼点的牌的范围即其手牌区里的一张牌）选择一张此次拼点的牌，再同时将各自此次拼点的牌扣置入处理区（各自此次拼点的牌的维系区域均为处理区）。

### 拼点牌被亮出时

然后同时亮出这些牌。

### （连续若干个）拼点结果确定后

然后确定发起者和T1此次拼点的结果并生成这两名角色的“拼点结果确定后”这个时机，再确定发起者和T2此次拼点的结果并生成这两名角色的“拼点结果确定后”这个时机……最后确定发起者和Tn此次拼点的结果并生成这两名角色的“拼点结果确定后”这个时机。确定两名角色的拼点结果的方法为：若这两名角色此次拼点的牌点数不同，则此次拼点的牌点数大的角色赢，此次拼点的牌点数小的角色未赢；若这两张牌点数相同，则这两名角色均未赢。

注：简而言之，和多名角色拼点时，发起拼点的角色用同一张牌和每个目标各拼一次。

至此拼点结算结束。

拼点结算后有：拼点结算结束后一个时机。

### 拼点结算结束后


至此拼点流程结束。

<!-- kb:refs:start -->

## 引用区

① API 实现：
- [PindianEvent](../../project-api/event/PindianEvent.md)
- [EventTypes](../../project-api/types/EventTypes.md)

② 扩展信息：
  （暂无）

③ 编写指南：
  （暂无）


<!-- kb:refs:end -->
